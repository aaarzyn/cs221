[project]
name = "cs221"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "einops>=0.8.1",
    "numpy>=2.3.3",
]

[tool.uv.workspace]
members = [
    "hw1_foundations",
]
